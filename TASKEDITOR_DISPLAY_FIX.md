# 🔧 TaskEditor 視窗顯示修正

## ❌ 問題描述
- 點擊「編」按鈕後，畫面變暗（遮罩顯示）
- 但 TaskEditor 視窗沒有出現

## ✅ 修正內容

### **1. TaskEditor 定位方式**
```typescript
// ❌ 錯誤：TaskEditor 內部使用 fixed 定位
style={{ 
  position: 'fixed',
  top: '50%',
  left: '50%',
  transform: 'translate(-50%, -50%)',
  zIndex: 2000  // 這個會被外層覆蓋
}}

// ✅ 正確：改為 relative，讓外層容器控制定位
style={{ 
  position: 'relative',
  width: '700px',
  maxHeight: '90vh',
  overflowY: 'auto',
  background: '#C0C0C0'
}}
```

### **2. 外層容器改用 Flexbox 置中**
```typescript
// ❌ 錯誤：兩個獨立的 div，定位複雜
<div style={{ zIndex: 10000 }} onClick={close}>遮罩</div>
<div style={{ zIndex: 10001, position: 'fixed' }}>視窗</div>

// ✅ 正確：遮罩使用 flexbox 置中
<div style={{ 
  position: 'fixed',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  zIndex: 10000
}}>
  <div onClick={(e) => e.stopPropagation()}>
    <TaskEditor />
  </div>
</div>
```

### **3. 加強 Console 追蹤**
- `[TaskEditor] 準備渲染 UI`
- `[TaskEditor] 開始渲染 JSX`
- `[SettingsPage] 點擊視窗內容（不關閉）`

---

## 🧪 測試步驟

### **1. 重新整理頁面**
按 `F5` 或 `Ctrl+R`

### **2. 開啟 Console**
按 `F12` → Console 分頁

### **3. 點擊「編」按鈕**
在任務項目列表中點擊任一任務的「編」按鈕

---

## 📊 預期結果

### **視覺效果**：
1. ✅ 畫面變暗（遮罩顯示）
2. ✅ **中央出現 Win95 風格的白色視窗**
3. ✅ 標題列顯示：`編輯任務 #105 - 日本出差`
4. ✅ 視窗內顯示完整的表單

### **Console 輸出**：
```
[SettingsPage] ========== 開啟任務編輯器 ==========
[SettingsPage] 任務 ID: 105
[SettingsPage] 任務標題: 日本出差
[SettingsPage] Render 檢查: { ..., taskToEdit: 'Task #105 - 日本出差' }
[SettingsPage] 渲染 TaskEditor，task: { id: 105, ... }
[TaskEditor] 組件初始化，編輯模式: true
[TaskEditor] 準備渲染 UI
[TaskEditor] 載入現有 schedule_config: { ... }
[TaskEditor] 開始渲染 JSX，task: 105 日本出差
```

---

## 🎨 視窗應該長這樣

```
┌─────────────────────────────────────────┐
│ 編輯任務 #105 - 日本出差                   │ ← 藍色標題列
├─────────────────────────────────────────┤
│ 📌 基本資訊                              │
│ ┌───────────────────────────────────┐   │
│ │ 任務名稱: [日本出差____________]    │   │
│ │ 說明: [____________________]      │   │
│ │ 積分: [10]  廠區: [ALL ▼]        │   │
│ └───────────────────────────────────┘   │
│                                          │
│ 🏷️ 任務分類                              │
│ ┌───────────────────────────────────┐   │
│ │ ○ 例行公事  ○ 交辦事項  ○ 公共事項 │   │
│ └───────────────────────────────────┘   │
│                                          │
│ 📅 排程設定                              │
│ ┌───────────────────────────────────┐   │
│ │ ○ 單次任務  ○ 區間任務  ○ 重複任務  │   │
│ └───────────────────────────────────┘   │
│                                          │
│ 📊 行事曆顯示方式                         │
│ ┌───────────────────────────────────┐   │
│ │ ○ 特殊事件 - 直接顯示               │   │
│ │ ○ 例行公事 - 摺疊顯示               │   │
│ │ ○ 週期任務 - 顯示                  │   │
│ └───────────────────────────────────┘   │
│                                          │
│              [取消]  [儲存變更]           │
└─────────────────────────────────────────┘
```

---

## 🐛 如果還是沒出現

請提供以下資訊：

### **1. Console 完整輸出**
從點擊「編」到現在的所有日誌

### **2. 檢查 DOM 元素**
按 `F12` → Elements 分頁
搜尋 `taskToEdit` 或 `TaskEditor`
看看元素是否存在但隱藏了

### **3. 瀏覽器資訊**
- Chrome / Edge / Firefox？
- 版本號？

### **4. 螢幕截圖**
點擊「編」後的畫面

---

## 💡 臨時解決方案

如果還是有問題，可以暫時用這個簡化版本測試：

在 SettingsPage.tsx 中，將 TaskEditor 改為簡單的 alert：

```typescript
const handleEditTask = (task: TaskDefinition) => {
  alert(`編輯任務 #${task.id} - ${task.title}`)
  // setTaskToEdit(task)  // 暫時註解
}
```

如果 alert 能彈出，就表示點擊事件正常，問題在於 TaskEditor 的渲染。

